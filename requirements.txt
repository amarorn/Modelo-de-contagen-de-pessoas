# Requer Python 3.8+ (sem pacote pip do ultralytics para Python 3.6/3.7)
ultralytics>=8.2.0
lap>=0.5.12
opencv-python>=4.8.0
pyyaml>=6.0.1
flask>=3.0.0
flask-cors>=4.0.0
sqlalchemy>=2.0.0
kafka-python>=2.0.2
# Necessario se DATABASE_URL usar postgresql+psycopg2:// (servidor Postgres a parte).
psycopg2-binary>=2.9.9
