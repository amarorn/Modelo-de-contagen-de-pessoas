apiVersion: v1
kind: Service
metadata:
  name: visioncount-backend
  labels:
    app.kubernetes.io/name: visioncount-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: visioncount-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
