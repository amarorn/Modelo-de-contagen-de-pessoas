apiVersion: v1
kind: Service
metadata:
  name: visioncount-frontend
  labels:
    app.kubernetes.io/name: visioncount-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: visioncount-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
